import { Dimensions } from "react-native";

const vh = Dimensions.get("screen").height
const vw = Dimensions.get("screen").width
const headerColor = "#F76756"

export {
    vh,
    vw,
    headerColor
}